# Student Management System
# This project uses only the Python standard library
# (sqlite3, csv, shutil, re, datetime) — no external
# packages are required to run it.
#
# This file is kept for future enhancements, e.g.:
# tabulate>=0.9.0      # nicer table formatting
# flask>=3.0.0          # for a future REST API
